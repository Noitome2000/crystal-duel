# 电脑如何下棋

电脑默认执靛方，玩家执赤方；开局也可选择本地双人。所有计算都在本机完成，不调用在线模型，也不需要网络。

## 先投币，再按顺序选将

投币使用浏览器 `crypto.getRandomValues` 生成均匀随机位：赤面赤方先手，靛面靛方先手。动画只展示结果，不影响概率。

选将顺序固定为：先手 1 号位 → 后手 1 号位 → 先手 2 号位 → 后手 2 号位。1 号位位于上方英雄格，2 号位位于下方英雄格。第四次选择完成后自动开局，由硬币决定的先手行动。

电脑选将使用一个独立的搭配评分：英雄基础权重约为 7～10 分，将军与骑士、军师与刺客、先锋与骑士有组合加分；也会参考对方已经选出的英雄。按评分进行加权随机抽选（softmax 温度 3），所有未选英雄都有机会，高分搭配概率略高，不再固定取最高分。它不会在轮到自己之前选将，同一阵营不会选重复英雄。这个阶段使用简单启发式，不是完整阵容博弈求解。

## 对局决策

1. **枚举合法行动。** 调用玩家同一份 `rules-engine.js`，列出移动、攻击、技能、结束连击、放弃游击、承受攻击，以及无合法行动时的交接。开局保护、冷却、路径和陷阱都照常生效。
2. **在副本上试走。** 每个候选行动都复制棋局，再通过规则引擎执行。因此吃子、换位、友军伤害、蓄力、胜负与后续阶段均由实际规则决定，不修改真实对局。
3. **评估局面。** 士兵基础价值 100，英雄 180；获胜价值 100000。另加中央位置、普通移动空间、攻击威胁，减去受困影响。巨龙蓄力会考虑未来落点的敌我净伤害。最近重复局面会被扣分，减少来回走动。
4. **试算双方回应。** 使用迭代加深的 Minimax 和 Alpha–Beta 剪枝。通常最多搜索四层决策；遇到连击、闪避、瞬闪后锁定攻击者的追加行动、游击或强制轰炸，会在深度末端最多再延伸两层。当前决策属于电脑时取较高分，属于玩家时假设玩家选较低分的回应。游侠防守时，决策方取防守者，不错误沿用进攻方的回合标记。
5. **限制开销并执行。** 每层先按静态评分排序；根节点保留评分靠前的 20 个候选，其余节点保留前 10 个；攻击、轰炸、闪避及即时胜负候选额外保留。一次思考最多约 4500 个生成节点或 750 毫秒；按实际设备可能有小幅调度超时。定期让出浏览器线程，棋盘旋转和页面仍可响应。若预算不足，使用上一轮完整搜索的最佳行动，最低也能返回合法备选。

连击、瞬闪、游击和轰炸都是独立的决策阶段。电脑会在需要时再次思考，玩家该做反应时则停下来等玩家。重新部署或打开规则会取消当前思考；旧结果不会落入新对局。

## 局限

这是适合小棋盘的离线启发式 AI。候选裁剪和有限搜索深度会漏掉较远的战术，也没有残局库或自我训练；它并不保证最优解。评分权重、深度、候选数和时间预算集中在 `ai.js`，可以继续调整。

## 验证

- `node --test tests/rules.test.cjs tests/ai.test.cjs tests/draft.test.cjs`
- `node tests/browser.cjs`
- `node tests/ui-ai.cjs`
- `node tests/draft-browser.cjs`

覆盖：规则与预算限制、获胜吃子、狼灭避免无谓友伤、双方游侠反应权、巨龙强制轰炸、连击、五组阵容的电脑对局、两种投币结果、四次选将顺序、1/2 号位部署、轮盘跟随和手机边界、电脑自动行动、重开取消旧行动。

瞬闪后原攻击者占据游侠原来的格子，从那里重新枚举行动；能够合法追击时会评估继续吃掉游侠。仍遵守攻击距离：普通两格攻击的英雄不能直接攻击相邻一格的游侠；刺客、尖兵或有合法三格绕行的士兵可能可以。

游侠防守时，AI 只要存在合法瞬闪方向就会选择闪避，不会因有限深度预测不利而直接放弃；被军师设陷或本次追加行动禁止再次瞬闪时按规则承受攻击。

搜索到深度末端仍会继续检查最多两层吃子与反吃，降低贪吃后送子的风险。单次思考缓存局面评分与候选排序；待决攻击路径也计入缓存标识。局面评分增加士兵获得的共享能力、游侠可移动空间，以及当前行动方的攻击威胁。参数仍属启发式，不代表实测英雄强度。
